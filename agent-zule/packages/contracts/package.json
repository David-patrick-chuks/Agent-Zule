{
  "name": "@agent-zule/contracts",
  "version": "1.0.0",
  "description": "Agent Zule Smart Contracts - Conditional Permissions on Monad",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network monad-testnet",
    "verify": "hardhat run scripts/verify.ts --network monad-testnet",
    "dev": "hardhat node"
  },
  "dependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.8.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@metamask/smart-accounts": "^0.1.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
